#pragma once
#include "../csand.hh"
#include "string"
#include "iostream"

// save a `.json` file with specific parameters
void SaveGame(const char *name, const char *desc, int grid[GRID_WIDTH][GRID_HEIGHT]) {
  
}