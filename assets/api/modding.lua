api_settings = {
  permissions="limited", -- "off", no modding | "limited", mods cannot edit files | "full", mods can do _almost_ anything
  mod_configs={

  }, -- each mod can have its own config, which is added to this table
}